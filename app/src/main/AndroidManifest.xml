<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirePreventionApp2"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HabitacionesActivity -->
        <activity android:name=".HabitacionesActivity" />

        <!-- ExtintoresActivity -->
        <activity android:name=".ExtintoresActivity" />

        <activity android:name=".Habitacion1Activity" />
        <activity android:name=".Habitacion2Activity" />
        <activity android:name=".Habitacion3Activity" />
        <activity android:name=".Habitacion4Activity" />
        <activity android:name=".Interfaz"/>

    </application>

</manifest>
